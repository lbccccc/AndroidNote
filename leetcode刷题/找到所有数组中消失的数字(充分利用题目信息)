给定一个范围在  1 ≤ a[i] ≤ n ( n = 数组大小 ) 的 整型数组，数组中的元素一些出现了两次，另一些只出现一次。

找到所有在 [1, n] 范围之间没有出现在数组中的数字。

您能在不使用额外空间且时间复杂度为O(n)的情况下完成这个任务吗? 你可以假定返回的数组不算在额外空间内。

示例:

输入:
[4,3,2,7,8,2,3,1]

输出:
[5,6]

第一反应用hash表遍历然后重复的元素put的时候覆盖了也无所谓，最后在检查hash表但是题目要求不能用额外空间。
官方答案：
充分利用题目给出的数组中的值都在1到n的距离递增除了那些消失的元素，所以可以用这些值和数组的下标配合。把出现过的数当做数组的下标并将其的值给变负，
如果已经是负了则忽略，最后用一个list来存储数组里那些正数的值，这些正数的值的下标就是消失的元素。


充分利用题目信息！！！！！！！！！！！
充分利用题目信息！！！！！！！！！！！
充分利用题目信息！！！！！！！！！！！
2020 9.13 21.33
